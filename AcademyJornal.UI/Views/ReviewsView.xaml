<UserControl x:Class="AcademyJornal.UI.Views.ReviewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:AcademyJornal.UI.ViewModels"

             xmlns:local="clr-namespace:AcademyJornal.UI.Views"
             mc:Ignorable="d" 

             d:DesignHeight="450" d:DesignWidth="800">
   
        <UserControl.DataContext>
            <vm:ReviewViewModel/>
        </UserControl.DataContext>

    <Grid Margin="28,24" Background="White">
        <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="💬 Генератор отзывов"
                   FontSize="22" FontWeight="Bold"
                   Foreground="{DynamicResource AccentPurpleLightBrush}"
                   Margin="0,0,0,20"/>

            <Border Grid.Row="1"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1" CornerRadius="12" Padding="20"
                Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="Имя студента" FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                             Style="{DynamicResource InputBox}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,12,0">
                        <TextBlock Text="Модуль" FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding Modules}"
                              SelectedItem="{Binding SelectedModule}"
                              Style="{DynamicResource ComboStyle}" Foreground="{StaticResource BorderBrush}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="0,0,12,0">
                        <TextBlock Text="Балл (1-10)" FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding ScoreText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{DynamicResource InputBox}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Margin="0,0,12,0">
                        <TextBlock Text="% ДЗ" FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding HwPercentText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{DynamicResource InputBox}"/>
                    </StackPanel>

                    <Button Grid.Column="4"
                        Content="✨  Сгенерировать"
                        Style="{DynamicResource PrimaryButton}"
                        VerticalAlignment="Bottom"
                        Command="{Binding GenerateCommand}"/>
                </Grid>
            </Border>

            <Border Grid.Row="2"
                Background="#33E24B4A"
                BorderBrush="#E24B4A"
                BorderThickness="1" CornerRadius="8"
                Padding="12,8" Margin="0,0,0,12"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#E24B4A" FontSize="13"/>
            </Border>

            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Reviews}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1" CornerRadius="12"
                                Padding="20" Margin="0,0,0,16">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <TextBlock Text="👤 " FontSize="14"/>
                                        <TextBlock Text="{Binding StudentName}"
                                               FontSize="15" FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding ModuleName}"
                                               FontSize="13" Margin="10,2,0,0"
                                               Foreground="{DynamicResource AccentPurpleLightBrush}"/>
                                        <TextBlock Text="{Binding Score, StringFormat='   Балл: {0:F1}'}"
                                               FontSize="13" Margin="8,2,0,0"
                                               Foreground="{DynamicResource AccentPinkBrush}"/>
                                        <TextBlock Text="{Binding GeneratedAt, StringFormat='   {0}'}"
                                               FontSize="11" Margin="8,3,0,0"
                                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </StackPanel>

                                    <!-- Редактируемый текст отзыва -->
                                    <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{DynamicResource InputBox}"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         MinHeight="80"
                                         VerticalScrollBarVisibility="Auto"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <Button Content="📋  Копировать"
                                            Style="{DynamicResource PrimaryButton}"
                                            Padding="12,6"
                                            Command="{Binding DataContext.CopyCommand,
                                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"/>
                                        <Button Content="✕  Удалить"
                                            Style="{DynamicResource SecondaryButton}"
                                            Padding="12,6" Margin="8,0,0,0"
                                            Command="{Binding DataContext.DeleteCommand,
                                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

        </Grid>
</UserControl>
